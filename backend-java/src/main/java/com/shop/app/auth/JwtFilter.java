package com.shop.app.auth;

// keep as a helper class (not a Spring bean) to avoid double-filtering; Spring Security will handle authentication
public class JwtFilter {
    // Intentionally left blank. JwtFilter was replaced by JwtAuthenticationFilter integrated with Spring Security.
}
